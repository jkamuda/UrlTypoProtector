{
  "manifest_version": 2,

  "name": "URL typo protector",
  "description": "Prevents users from proceeding to a common mistakenly typed in URL",
  "version": "1.0",

  "background": {
    "scripts": ["background.js"]
  },
  "permissions": [
    "activeTab",
    "webRequest",
    "webRequestBlocking",
    "<all_urls>"
  ]
}